import DashboardTitle from "../shared/DashboardTitle";

const MaintainerSettings = () => {
  return (
    <section>
      <DashboardTitle heading="settings" description="" />
    </section>
  );
};

export default MaintainerSettings;
